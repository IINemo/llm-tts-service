# @package _global_
# Online Best-of-N with Thinking Mode + ThinkingMarkerDetector
# Uses semantic marker detection for step boundaries in <think> content

defaults:
  - /config
  - /dataset/aime_2025
  - /model/hf_qwen3
  - /generation/default
  - /system/default
  - _self_

# Run naming
run_name: "aime2025_online_bon_thinking_${now:%H-%M-%S}"

# Main configuration
verbose: true
report_to: wandb
wandb_project: llm-tts-eval-aime2025

# Model configuration
model:
  type: "local"
  model_path: "Qwen/Qwen3-8B"
  disable_thinking_mode: false  # ENABLE native thinking mode

# System configuration
system:
  device: cuda
  seed: 42
  torch_dtype: "bfloat16"

# Generation configuration (thinking mode settings)
generation:
  max_new_tokens: 4096
  temperature: 0.6
  top_p: 0.95
  top_k: 20
  batch_size: 1
  max_length: 8192

# Scorer - entropy-based uncertainty for step selection
scorer:
  type: uncertainty
  uncertainty_model_creator: config.scorer.uncertainty_entropy

# Strategy - Online Best-of-N with thinking marker detector
strategy:
  type: online_best_of_n
  candidates_per_step: 4
  max_steps: 10
  # Use ThinkingMarkerDetector for semantic step boundaries
  detector_type: thinking_marker
  min_step_chars: 50
  max_step_chars: 800
  detector_answer_patterns:
    - "</think>"
    - "<Answer>:"
    - "\\boxed{"

# Dataset configuration
dataset:
  subset: 5  # Start with 5 samples for testing
  offset: 0
  prompt_file: "${hydra:runtime.cwd}/config/prompts/default.txt"

# Evaluation configuration
evaluation:
  evaluators:
    - exact_match
