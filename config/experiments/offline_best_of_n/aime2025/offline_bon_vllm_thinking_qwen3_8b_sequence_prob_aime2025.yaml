# @package _global_
# Run: CUDA_VISIBLE_DEVICES=0 python scripts/run_tts_eval.py --config-path=../config --config-name=experiments/offline_best_of_n/aime2025/offline_bon_vllm_thinking_qwen3_8b_sequence_prob_aime2025
# Offline Best-of-N with Thinking Mode (Qwen3-8B) on AIME 2025 — Sequence Probability scorer
# Generates N complete trajectories (thinking + response), then selects the best one

defaults:
  - /config
  - /dataset/aime_2025
  - /model/vllm_qwen3
  - /generation/default
  - /system/default
  - /scorer/sequence_prob
  - _self_

# Run naming
run_name: "offline_bon_vllm_thinking_qwen3_8b_sequence_prob_aime2025_${now:%H-%M-%S}"

# Main configuration
verbose: true
report_to: wandb
wandb_project: llm-tts-eval-aime2025

# Model configuration (vLLM backend)
model:
  type: "vllm"
  model_path: "Qwen/Qwen3-8B"
  gpu_memory_utilization: 0.8
  tensor_parallel_size: 1
  enable_prefix_caching: true
  trust_remote_code: true
  max_model_len: 32768
  disable_thinking_mode: false  # ENABLE native thinking mode

# System configuration
system:
  device: cuda
  seed: 42

# Generation configuration (thinking mode settings)
generation:
  max_new_tokens: 4096
  temperature: 0.6
  top_p: 0.95
  top_k: 20
  batch_size: 1
  max_length: 32768

# Strategy - Offline Best-of-N
strategy:
  type: offline_best_of_n
  num_trajectories: 4
  max_thinking_tokens: 32768
  max_response_tokens: 1024
  detector_answer_patterns:
    - "<end of response>"
  min_step_tokens: 125
  max_step_tokens: 500
  use_sequence: true
  use_conclusion: true
  use_thinking: true
  use_verification: true
  use_structure: false
  use_reasoning: true
  use_correction: false

# Sequence Probability scorer configuration
scorer:
  type: sequence_prob
  batch_size: 1

# Dataset configuration
dataset:
  subset: 30
  offset: 0
  data_name: aime2025
  system_prompt: "Please reason step by step, and put your final answer within \\boxed{}."

# Evaluation configuration
evaluation:
  evaluators:
    - exact_match
  batch_evaluators:
    - llm_judge
  llm_judge:
    provider: openai
    model: gpt-4o-mini
    base_url: https://api.openai.com/v1
    cache_path: ./cache/llm_judge
    n_threads: 10
