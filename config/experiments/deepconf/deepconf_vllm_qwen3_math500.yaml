# @package _global_
# Run: CUDA_VISIBLE_DEVICES=0 python scripts/run_tts_eval.py --config-path=../config --config-name=experiments/deepconf/deepconf_vllm_qwen3_math500
# Note: Do NOT specify CUDA_VISIBLE_DEVICES when submitting via scripts/slurm/submit.sh - SLURM assigns GPUs automatically
# DeepConf Offline Mode: Qwen3-8B via vLLM on MATH-500

defaults:
  - /config
  - /dataset/math_500
  - /model/vllm_qwen3
  - /generation/default
  - /system/default
  - /evaluation/default
  - _self_

# Run naming
run_name: "deepconf_vllm_qwen3_math500_seed${system.seed}_${now:%H-%M-%S}"

# Main configuration
verbose: true
report_to: null  # Disable wandb for local runs

# Model configuration (vLLM backend)
model:
  type: "vllm"
  model_path: "Qwen/Qwen3-8B"
  model_short_name: "qwen3_8b"
  gpu_memory_utilization: 0.9
  tensor_parallel_size: 1
  enable_prefix_caching: true
  trust_remote_code: true
  max_context_budget: 32768
  disable_thinking_mode: true  # Disable Qwen3 thinking mode

# System configuration
system:
  device: cuda
  seed: 42

# Generation configuration (non-thinking mode: temp=0.7, top_p=0.8, top_k=20)
generation:
  max_new_tokens: 32768
  temperature: 0.7
  top_p: 0.8
  top_k: 20
  batch_size: 1
  max_length: 8192

# No scorer needed (DeepConf has built-in weighted voting)
scorer: null

# DeepConf offline mode strategy
strategy:
  type: deepconf
  mode: "offline"  # Generate all traces, filter by confidence, weighted vote
  budget: 16  # Number of reasoning traces to generate

  # Generation parameters
  window_size: 2048  # Sliding window for confidence computation
  temperature: 0.7  # Sampling temperature for diversity
  top_p: 0.8  # Nucleus sampling
  max_tokens: 32768  # vLLM handles long sequences efficiently
  top_logprobs: 20  # Required for confidence computation

  # Filtering and voting
  filter_method: "top10"  # Use top-10 traces by confidence for voting
  n_threads: 1  # Not used for vLLM (single batched call)

# Dataset configuration
dataset:
  subset: 500  # All MATH-500 problems
  offset: 0
  question_field: "problem"
  answer_field: "answer"
  prompt_file: "${hydra:runtime.cwd}/config/prompts/default_relaxed.txt"
