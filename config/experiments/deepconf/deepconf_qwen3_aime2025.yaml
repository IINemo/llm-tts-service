# @package _global_
# DeepConf Offline Mode: Qwen3-8B (local) on AIME 2025
# Competition-level mathematics problems with confidence-based test-time scaling

defaults:
  - /config
  - /dataset/aime_2025
  - /model/hf_qwen3
  - /generation/default
  - /system/default
  - _self_

# Run naming
run_name: "aime2025_deepconf_qwen3_b${strategy.budget}_${now:%H-%M-%S}"

# Main configuration
verbose: true
report_to: null  # Disable wandb for local runs

# Model configuration (local HuggingFace model)
model:
  type: "local"
  model_path: "Qwen/Qwen3-8B"
  disable_thinking_mode: true
  device: cuda:1

# System configuration
system:
  device: cuda:1
  seed: 42

# Generation configuration
generation:
  max_new_tokens: 32768  # AIME problems require more tokens
  temperature: 0.7
  top_p: 0.95
  batch_size: 1
  max_length: 8192

# No scorer needed (DeepConf has built-in weighted voting)
scorer: null

# DeepConf offline mode strategy
strategy:
  type: deepconf
  mode: "offline"  # Generate all traces, filter by confidence, weighted vote
  budget: 16  # Number of reasoning traces to generate

  # Generation parameters
  window_size: 2048  # Sliding window for confidence computation
  temperature: 0.6  # Sampling temperature for diversity
  top_p: 0.95  # Nucleus sampling
  max_tokens: 32768  # Match AIME complexity
  top_logprobs: 20  # Required for confidence computation

  # Filtering and voting
  filter_method: "top10"  # Use top-10 traces by confidence for voting
  n_threads: 1  # Sequential generation for local model

# Dataset configuration
dataset:
  subset: 30  # All AIME 2025 problems
  offset: 0  # Start from beginning
  prompt_file: "${hydra:runtime.cwd}/config/prompts/default.txt"

# Evaluation configuration
evaluation:
  evaluators:
    - exact_match
