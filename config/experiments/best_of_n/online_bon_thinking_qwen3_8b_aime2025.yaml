# @package _global_
# Online Best-of-N with Thinking Mode + ThinkingMarkerDetector
# Uses semantic marker detection for step boundaries in <think> content

defaults:
  - /config
  - /dataset/aime_2025
  - /model/hf_qwen3
  - /generation/default
  - /system/default
  - /scorer/entropy
  - _self_

# Run naming
run_name: "aime2025_online_bon_qwen3_8b_thinking_${now:%H-%M-%S}"

# Main configuration
verbose: true
report_to: wandb
wandb_project: llm-tts-eval-aime2025

# Model configuration
model:
  type: "local"
  model_path: "Qwen/Qwen3-8B"
  disable_thinking_mode: false  # ENABLE native thinking mode

# System configuration
system:
  device: cuda
  seed: 42
  torch_dtype: "bfloat16"

# Generation configuration (thinking mode settings)
generation:
  max_new_tokens: 4096
  temperature: 0.6
  top_p: 0.95
  top_k: 20
  batch_size: 1
  max_length: 8192

# Strategy - Online Best-of-N with thinking marker detector
strategy:
  type: online_best_of_n
  candidates_per_step: 4
  max_steps: 250
  # Use ThinkingMarkerDetector with marker_semantic_v2 settings
  detector_type: thinking_marker
  # Step boundary token limits (same as vLLM config)
  min_step_tokens: 50
  max_step_tokens: 300
  # Marker categories
  use_sequence: true
  use_conclusion: true
  use_thinking: true
  use_verification: true
  use_structure: false       # No paragraph/bullet splitting
  use_reasoning: false       # REASONING_MARKERS has too many patterns
  use_sentence_start: false  # Avoid "but", "however" over-splitting
  use_correction: false
  # Only the 3 selective marker_semantic_v2 additions
  custom_markers:
    - "\\bfor example\\b"
    - "\\bgiven that\\b"
    - "\\bsimilarly\\b"
  detector_answer_patterns:
    - "<end of response>"

# Dataset configuration
dataset:
  subset: 30
  offset: 0
  prompt_file: "${hydra:runtime.cwd}/config/prompts/default_relaxed.txt"

# Evaluation configuration
evaluation:
  evaluators:
    - exact_match
