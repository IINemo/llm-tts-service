# @package _global_
# Run: CUDA_VISIBLE_DEVICES=0 python scripts/run_tts_eval.py --config-path=../config --config-name=experiments/online_best_of_n/math500/online_bon_vllm_nothink_qwen25_7b_math500_sequence_prob
# Online Best-of-N with vLLM + Non-Thinking Mode (Qwen2.5-7B) on MATH-500
# Uses Sequence Probability scorer for step evaluation

defaults:
  - /config
  - /dataset/math_500
  - /model/vllm_qwen3
  - /generation/default
  - /system/default
  - /scorer/sequence_prob
  - /evaluation/default
  - _self_

# Run naming
run_name: "seed${system.seed}_${now:%H-%M-%S}"

# Main configuration
verbose: true
report_to: wandb
wandb_project: llm-tts-eval-math500
wandb_group: "online_bon_qwen25_math_7b_instruct_math500_sequence_prob"
# Model configuration (vLLM backend)
model:
  type: "vllm"
  model_path: "Qwen/Qwen2.5-Math-7B-Instruct"
  model_short_name: "qwen25_math_7b_instruct"
  gpu_memory_utilization: 0.9
  tensor_parallel_size: 1
  enable_prefix_caching: true
  trust_remote_code: true
  max_context_budget: 4096
  # disable_thinking_mode not set (None) = model doesn't support thinking

# System configuration
system:
  device: cuda
  seed: 42

# Generation configuration (non-thinking mode settings)
generation:
  max_new_tokens: 4096
  temperature: 0.7
  top_p: 0.8
  top_k: 20
  batch_size: 1
  max_length: 8192

# Strategy - Online Best-of-N (always uses ThinkingMarkerDetector)
strategy:
  type: online_best_of_n
  candidates_per_step: 8
  max_steps: 50
  # Stop tokens derived from detector config (use_* flags below)
  stop_token_ids:
    - 151645  # <|im_end|>
    - 151643  # <|endoftext|>
  # Step boundary token limits
  min_step_tokens: 50
  max_step_tokens: 2048
  # Marker categories (ThinkingMarkerDetector derives stop tokens from these)
  use_sequence: true
  use_conclusion: true
  use_thinking: true  # Include thinking markers for step boundaries
  use_verification: true
  use_structure: true
  use_reasoning: true
  use_correction: false
  # Custom words for stop tokens
  custom_words:
    - "for example"
    - "given that"
    - "similarly"
    - "step"
    - "therefore"
    - "thus"

# Dataset configuration
dataset:
  subset: 500  # Full MATH-500 dataset
  data_name: math
  offset: 0
  prompt_file: "${hydra:runtime.cwd}/config/prompts/qwen25_math_official.txt"
