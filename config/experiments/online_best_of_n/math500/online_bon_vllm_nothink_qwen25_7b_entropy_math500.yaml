# @package _global_
# Run: CUDA_VISIBLE_DEVICES=0 python scripts/run_tts_eval.py --config-path=../config --config-name=experiments/online_best_of_n/math500/online_bon_vllm_nothink_qwen25_7b_entropy_math500
# Online Best-of-N with vLLM + Non-Thinking Mode (Qwen2.5-7B) on MATH-500
# Uses standard generation without thinking markers

defaults:
  - /config
  - /dataset/math_500
  - /model/vllm_qwen3
  - /generation/default
  - /system/default
  - /scorer/entropy
  - _self_

# Run naming
run_name: "online_bon_vllm_nothink_qwen25_7b_entropy_math500_${now:%H-%M-%S}"

# Main configuration
verbose: true
report_to: wandb
wandb_project: llm-tts-eval-math500

# Model configuration (vLLM backend)
model:
  type: "vllm"
  model_path: "Qwen/Qwen2.5-7B"
  gpu_memory_utilization: 0.8
  tensor_parallel_size: 1
  enable_prefix_caching: true
  trust_remote_code: true
  max_model_len: 32768
  disable_thinking_mode: true  # No native thinking mode

# System configuration
system:
  device: cuda
  seed: 42

# Generation configuration (non-thinking mode settings)
generation:
  max_new_tokens: 4096
  temperature: 0.7
  top_p: 0.8
  top_k: 20
  batch_size: 1
  max_length: 8192

# Strategy - Online Best-of-N with structured step detector
strategy:
  type: online_best_of_n
  candidates_per_step: 4
  max_steps: 50
  # Use structured detector for non-thinking mode
  detector_type: structured
  # Step boundary token limits
  min_step_tokens: 50
  max_step_tokens: 300
  # Marker categories (for vLLM stop token generation)
  use_sequence: true
  use_conclusion: true
  use_thinking: false  # No thinking markers in non-thinking mode
  use_verification: true
  use_structure: true
  use_reasoning: true
  use_correction: false
  # Custom words for stop tokens
  custom_words:
    - "for example"
    - "given that"
    - "similarly"
    - "step"
    - "therefore"
    - "thus"
  # Answer patterns for checking if there's actual answer content
  detector_answer_patterns:
    - "<Answer>:"
    - "\n<Answer>:"
  # EOS patterns for checking trajectory completion (stop tokens)
  detector_eos_patterns:
    - "<end of response>"

# Dataset configuration
dataset:
  subset: 500  # Full MATH-500 dataset
  offset: 0
  prompt_file: "${hydra:runtime.cwd}/config/prompts/default_relaxed_qwen2.5.txt"

# Evaluation configuration
evaluation:
  evaluators:
    - exact_match
