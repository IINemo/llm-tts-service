# @package _global_
# Run: CUDA_VISIBLE_DEVICES=0 python scripts/run_tts_eval.py --config-path=../config --config-name=experiments/online_best_of_n/aime2024/vllm/online_bon_vllm_thinking_qwen3_8b_aime2024_pd
# Note: Do NOT specify CUDA_VISIBLE_DEVICES when submitting via scripts/slurm/submit.sh - SLURM assigns GPUs automatically
# Online Best-of-N with vLLM + Thinking Mode (Qwen3-8B) on AIME 2024
# Uses PD (Predictive Diversity) scorer for step evaluation

defaults:
  - /config
  - /model/vllm_qwen3
  - /generation/default
  - /system/default
  - /scorer/uncertainty_pd
  - /evaluation/default
  - _self_

# Run naming
run_name: "online_bon_vllm_thinking_qwen3_8b_aime2024_pd_seed${system.seed}_${now:%H-%M-%S}"

# Main configuration
verbose: true
report_to: wandb
wandb_project: llm-tts-eval-aime2024
wandb_group: "online_bon_qwen3_8b_thinking_aime2024_pd"
# Model configuration (vLLM backend)
model:
  type: "vllm"
  model_path: "Qwen/Qwen3-8B"
  model_short_name: "vllm_thinking_qwen3_8b"
  gpu_memory_utilization: 0.8
  tensor_parallel_size: 1
  enable_prefix_caching: true
  trust_remote_code: true
  max_context_budget: 32768
  disable_thinking_mode: false  # ENABLE native thinking mode

# System configuration
system:
  device: cuda
  seed: 42

# Generation configuration (thinking mode settings)
generation:
  max_new_tokens: 4096
  temperature: 0.6
  top_p: 0.95
  top_k: 20
  batch_size: 1
  checkpoint_batch_size: 4
  max_length: 8192

# Strategy - Online Best-of-N with thinking marker detector
strategy:
  type: online_best_of_n
  candidates_per_step: 8
  max_steps: 250
  # Use VLLMStepGenerator in thinking mode
  detector_type: thinking_marker
  # Step boundary token limits
  min_step_tokens: 50
  max_step_tokens: 300
  # Marker categories (for vLLM stop token generation)
  use_sequence: true
  use_conclusion: true
  use_thinking: true
  use_verification: true
  use_structure: false
  use_reasoning: true
  use_correction: false
  # Custom words for stop tokens
  custom_words:
    - "for example"
    - "given that"
    - "similarly"
  detector_answer_patterns:
    - "<end of response>"

# Dataset configuration - AIME 2024 (30 problems)
dataset:
  dataset_path: "test-time-compute/test_aime24"
  dataset_config: null
  dataset_split: "test"
  subset: 30
  offset: 0
  question_field: "question"
  answer_field: "answer"
  answer_format: "numeric"
  data_name: "aime24"
  system_prompt: "Please reason step by step, and put your final answer within \\boxed{}."
  prompt_template: "{question}"
  prompt_file: null
