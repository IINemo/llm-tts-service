# @package _global_
# Run: CUDA_VISIBLE_DEVICES=0 python scripts/run_tts_eval.py --config-path=../config --config-name=experiments/online_best_of_n/aime2025/online_bon_hybrid_entropy_aime2025
# Online Best-of-N with Hybrid Generator (vLLM generation + HuggingFace scoring)
# Uses vLLM for fast generation with PagedAttention, HuggingFace for uncertainty scoring

defaults:
  - /config
  - /dataset/aime_2025
  - /generation/default
  - /system/default
  - /scorer/entropy
  - _self_

# Run naming
run_name: "online_bon_hybrid_entropy_aime2025_${now:%H-%M-%S}"

# Main configuration
verbose: true
report_to: wandb
wandb_project: llm-tts-eval-aime2025

# Model configuration - HYBRID mode
model:
  type: "hybrid"  # vLLM for generation, HuggingFace for scoring
  model_path: "Qwen/Qwen3-8B"
  disable_thinking_mode: false  # ENABLE native thinking mode
  # vLLM settings (for generation)
  vllm_gpu_memory_utilization: 0.9  # Full GPU for vLLM on GPU 0
  tensor_parallel_size: 1
  enable_prefix_caching: true
  max_model_len: 32768
  # HuggingFace settings (for scoring on GPU 1)
  hf_device: "cuda:1"
  hf_dtype: "bfloat16"  # Use same dtype as vLLM for consistent logprobs
  output_hidden_states: true  # Enable hidden states for advanced UQ methods
  output_attentions: false  # Disable attention weights to save memory

# System configuration
system:
  device: cuda
  seed: 42
  torch_dtype: "bfloat16"

# Generation configuration (thinking mode settings)
generation:
  max_new_tokens: 4096
  temperature: 0.6
  top_p: 0.95
  top_k: 20
  batch_size: 1
  max_length: 8192

# Strategy - Online Best-of-N with thinking marker detector
strategy:
  type: online_best_of_n
  candidates_per_step: 4
  max_steps: 250
  # Use ThinkingMarkerDetector with marker_semantic_v2 settings
  detector_type: thinking_marker
  # Step boundary token limits
  min_step_tokens: 50
  max_step_tokens: 300
  # Marker categories
  use_sequence: true
  use_conclusion: true
  use_thinking: true
  use_verification: true
  use_structure: false
  use_reasoning: false
  use_sentence_start: false
  use_correction: false
  custom_markers:
    - "\\bfor example\\b"
    - "\\bgiven that\\b"
    - "\\bsimilarly\\b"
  detector_answer_patterns:
    - "<end of response>"

# Dataset configuration
dataset:
  subset: null  # Use full dataset
  offset: 0
  prompt_file: "${hydra:runtime.cwd}/config/prompts/default_relaxed.txt"

# Evaluation configuration
evaluation:
  evaluators:
    - exact_match
