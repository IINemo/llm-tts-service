# @package _global_
# Run: CUDA_VISIBLE_DEVICES=0 python scripts/run_tts_eval.py --config-path=../config --config-name=experiments/online_best_of_n/aime2025/hf/online_bon_hf_structured_aime2025_entropy
# Online Best-of-N with Structured Mode (no thinking)
# Uses StructuredStepDetector for step boundaries like "- Step N:"

defaults:
  - /config
  - /dataset/aime_2025
  - /model/hf_qwen3
  - /generation/default
  - /system/default
  - /scorer/entropy
  - _self_

# Run naming
run_name: "online_bon_hf_structured_aime2025_entropy_${now:%H-%M-%S}"

# Main configuration
verbose: true
report_to: wandb
wandb_project: llm-tts-eval-aime2025

# Model configuration
model:
  type: "local"
  model_path: "Qwen/Qwen3-8B"
  disable_thinking_mode: true  # DISABLE thinking mode
  gpu_memory_utilization: 0.9  # Limit GPU memory to avoid OOM

# System configuration
system:
  device: cuda
  seed: 42
  torch_dtype: "bfloat16"

# Generation configuration
generation:
  max_new_tokens: 4096
  temperature: 0.6
  top_p: 0.95
  top_k: 20
  batch_size: 1
  max_length: 8192

# Strategy - Online Best-of-N with structured step detector
strategy:
  type: online_best_of_n
  candidates_per_step: 8
  max_steps: 250
  # Use StructuredStepDetector for non-thinking mode
  detector_type: structured
  use_thinking: true
  # Step patterns for structured mode
  detector_step_patterns:
    - "- Step"
    - "<Answer>:"
    - "\n<Answer>:"
  detector_answer_patterns:
    - "<Answer>:"
    - "\n<Answer>:"
    - "<end of response>"

# Dataset configuration
dataset:
  subset: 30
  offset: 0
  prompt_file: "${hydra:runtime.cwd}/config/prompts/default_relaxed.txt"

# Evaluation configuration
evaluation:
  # Phase 1 evaluators (run during generation for running accuracy)
  evaluators:
    - exact_match

  # Phase 2 evaluators (batch evaluation after generation)
  batch_evaluators:
    - llm_judge

  # API-based LLM judge configuration
  llm_judge:
    provider: openai
    model: gpt-4o-mini
    base_url: https://api.openai.com/v1
    cache_path: ./cache/llm_judge
    n_threads: 50
