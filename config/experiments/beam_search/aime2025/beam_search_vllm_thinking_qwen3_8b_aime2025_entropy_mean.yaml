# @package _global_
# Run: CUDA_VISIBLE_DEVICES=0 python scripts/run_tts_eval.py --config-path=../config --config-name=experiments/beam_search/aime2025/beam_search_vllm_thinking_qwen3_8b_aime2025_entropy_mean
# Note: Do NOT specify CUDA_VISIBLE_DEVICES when submitting via scripts/slurm/submit.sh - SLURM assigns GPUs automatically
# Beam Search with Thinking Mode (Qwen3-8B) + Entropy scorer on AIME 2025
# Uses thinking mode with semantic stop tokens for step boundaries
# Aggregation: mean (balances all step scores)

defaults:
  - /config
  - /model/vllm_qwen3
  - /generation/default
  - /system/default
  - /strategy/beam_search
  - /scorer/entropy
  - /evaluation/default
  - _self_

# Run naming - note: min/mean subfolder is determined by run_name structure
run_name: "beam_search/vllm_thinking_qwen3_8b/aime2025/entropy/mean/seed${system.seed}_${now:%H-%M-%S}"

# Main configuration
verbose: true
report_to: wandb
wandb_project: llm-tts-eval-aime2025
wandb_group: "beam_search_qwen3_8b_thinking_aime2025_entropy_mean"

# Model configuration (vLLM backend)
model:
  type: "vllm"
  model_path: "Qwen/Qwen3-8B"
  model_short_name: "vllm_thinking_qwen3_8b"
  gpu_memory_utilization: 0.85
  tensor_parallel_size: 1
  enable_prefix_caching: true
  trust_remote_code: true
  max_context_budget: 32768
  disable_thinking_mode: false  # ENABLE native thinking mode

# System configuration
system:
  device: cuda
  seed: 42

# Generation configuration (thinking mode settings)
generation:
  max_new_tokens: 32768
  temperature: 0.6
  top_p: 0.95
  top_k: 20
  batch_size: 1
  max_length: 65536

# Strategy - Beam Search with mean aggregation
strategy:
  beam_size: 3
  candidates_per_beam: 5
  max_steps: 250
  aggregation: mean  # Balance all step scores

# Entropy scorer configuration
scorer:
  type: entropy
  batch_size: 1

# Dataset configuration - AIME 2025
dataset:
  dataset_path: "test-time-compute/aime_2025"
  dataset_config: "default"
  dataset_split: "test"
  subset: 30  # Full AIME 2025 dataset
  offset: 0
  question_field: "question"
  answer_field: "answer"
  answer_format: "numeric"
  data_name: aime25
  system_prompt: "Please reason step by step, and put your final answer within \\boxed{}."
  prompt_template: "{question}"
  prompt_file: null
