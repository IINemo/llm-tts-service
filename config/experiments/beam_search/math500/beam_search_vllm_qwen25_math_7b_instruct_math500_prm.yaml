# @package _global_
# Run: CUDA_VISIBLE_DEVICES=0,1 python scripts/run_tts_eval.py --config-path=../config --config-name=experiments/beam_search/math500/beam_search_vllm_nothink_qwen25_7b_math500_prm
# Note: Do NOT specify CUDA_VISIBLE_DEVICES when submitting via scripts/slurm/submit.sh - SLURM assigns GPUs automatically
# Beam Search with vLLM + Non-Thinking Mode (Qwen2.5-7B) on MATH-500
# Uses PRM scorer (Qwen2.5-Math-PRM-7B) for step evaluation
# Maintains B beams, expands each with N candidates, prunes to top-B at each step

defaults:
  - /config
  - /dataset/math_500
  - /model/vllm_qwen3
  - /generation/default
  - /system/default
  - /strategy/beam_search
  - /scorer/prm
  - /evaluation/default
  - _self_

# Run naming
run_name: "beam_search_vllm_nothink_qwen25_7b_math500_prm_seed${system.seed}_${now:%H-%M-%S}"

# Main configuration
verbose: true
report_to: wandb
wandb_project: llm-tts-eval-math500
wandb_group: "beam_search_qwen25_math_7b_instruct_math500_prm"
# Model configuration (vLLM backend)
model:
  type: "vllm"
  model_path: "Qwen/Qwen2.5-Math-7B-Instruct"
  model_short_name: "qwen25_math_7b_instruct"
  gpu_memory_utilization: 0.95
  tensor_parallel_size: 1
  enable_prefix_caching: true
  trust_remote_code: true
  max_context_budget: 4096  # Qwen2.5-Math has 4K context limit

# System configuration
system:
  device: cuda
  seed: 42

# Generation configuration (non-thinking mode settings)
generation:
  max_new_tokens: 4096
  temperature: 0.7
  top_p: 0.8
  top_k: 20
  batch_size: 1
  max_length: 8192

# Strategy - Beam Search
strategy:
  beam_size: 5              # Number of top beams to keep at each step
  candidates_per_beam: 8    # Number of candidates to generate per beam
  max_steps: 30             # Maximum reasoning steps
  # Stop tokens derived from detector config
  stop_token_ids:
    - 151645  # <|im_end|>
    - 151643  # <|endoftext|>
  # Step boundary token limits
  max_step_tokens: 256
  # Marker categories (ThinkingMarkerDetector derives stop tokens from these)
  # Custom words for stop tokens

# PRM scorer configuration
scorer:
  type: prm
  model_path: "Qwen/Qwen2.5-Math-PRM-7B"
  device: cuda:1
  batch_size: 1

# Dataset configuration - MATH-500
dataset:
  subset: 500  # Full MATH-500 dataset
  offset: 0
  data_name: math  # For official Qwen2.5-Math answer extraction
  prompt_file: "${hydra:runtime.cwd}/config/prompts/qwen25_math_official.txt"
