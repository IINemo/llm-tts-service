# @package _global_
# Run: CUDA_VISIBLE_DEVICES=0 python scripts/run_tts_eval.py --config-path=../config --config-name=experiments/beam_search/math500/beam_search_vllm_nothink_qwen25_7b_math500_sequence_prob
# Note: Do NOT specify CUDA_VISIBLE_DEVICES when submitting via scripts/slurm/submit.sh - SLURM assigns GPUs automatically
# Beam Search with vLLM + Non-Thinking Mode (Qwen2.5-7B) on MATH-500
# Uses Sequence Probability scorer for step evaluation (sum of log-probs, not normalized)
# Maintains B beams, expands each with N candidates, prunes to top-B at each step

defaults:
  - /config
  - /dataset/math_500
  - /model/vllm_qwen3
  - /generation/default
  - /system/default
  - /strategy/beam_search
  - /scorer/sequence_prob
  - /evaluation/default
  - _self_

# Run naming
run_name: "beam_search_vllm_nothink_qwen25_7b_math500_sequence_prob_seed${system.seed}_${now:%H-%M-%S}"

# Main configuration
verbose: true
report_to: wandb
wandb_project: llm-tts-eval-math500
wandb_group: "beam_search_qwen25_math_7b_instruct_math500_sequence_prob"
# Model configuration (vLLM backend)
# Single GPU since sequence_prob scorer uses the same model (no separate PRM)
model:
  type: "vllm"
  model_path: "Qwen/Qwen2.5-Math-7B-Instruct"
  model_short_name: "qwen25_math_7b_instruct"
  gpu_memory_utilization: 0.9
  tensor_parallel_size: 1
  enable_prefix_caching: true
  trust_remote_code: true
  max_context_budget: 4096

# System configuration
system:
  device: cuda
  seed: 42

# Generation configuration (non-thinking mode settings)
generation:
  max_new_tokens: 4096
  temperature: 0.7
  top_p: 0.8
  top_k: 20
  batch_size: 1
  max_length: 8192

# Strategy - Beam Search
strategy:
  beam_size: 5
  candidates_per_beam: 8
  max_steps: 30
  stop_token_ids:
    - 151645  # <|im_end|>
    - 151643  # <|endoftext|>
  max_step_tokens: 256
  use_sequence: true
  use_conclusion: true
  use_thinking: true
  use_verification: true
  use_structure: false
  use_reasoning: true
  use_correction: false

# Dataset configuration
dataset:
  subset: 500
  offset: 0
  data_name: math
  prompt_file: "${hydra:runtime.cwd}/config/prompts/qwen25_math_official.txt"

# Sequence probability scorer configuration (uses same model, no separate GPU needed)
scorer:
  type: sequence_prob
  batch_size: 1
