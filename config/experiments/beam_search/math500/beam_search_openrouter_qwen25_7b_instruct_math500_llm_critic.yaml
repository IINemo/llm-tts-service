# @package _global_
# Run: python scripts/run_tts_eval.py --config-path=../config --config-name=experiments/beam_search/math500/beam_search_openrouter_qwen25_7b_instruct_math500_llm_critic
#
# Beam Search with LLM Critic Scorer (Tree of Thoughts paper)
# Uses OpenRouter API - no GPU required!
#
# Based on: "Tree of Thoughts: Deliberate Problem Solving with Large Language Models"
# Paper: https://arxiv.org/abs/2305.10601

defaults:
  - /config
  - /dataset/math_500
  - /model/openrouter
  - /generation/default
  - /system/default
  - /strategy/beam_search
  - /scorer/llm_critic
  - /evaluation/default
  - _self_

# Run naming
run_name: "beam_search_openrouter_qwen25_7b_instruct_math500_llm_critic_${now:%H-%M-%S}"

# Main configuration
verbose: true
report_to: wandb
wandb_project: llm-tts-eval-math500
wandb_group: "beam_search_openrouter_qwen25_7b_instruct_math500_llm_critic"

# Model overrides (Qwen2.5-7B-Instruct via OpenRouter)
model:
  model_name: "qwen/qwen-2.5-7b-instruct"
  model_short_name: "openrouter_qwen25_7b_instruct"
  supports_logprobs: false
  disable_thinking_mode: true

# System override (no GPU needed for API)
system:
  device: cpu

# Generation overrides
generation:
  max_new_tokens: 2048
  max_length: 4096
  top_p: 0.9
  top_k: 50

# Strategy overrides (smaller scale for API)
strategy:
  beam_size: 3
  candidates_per_beam: 3
  max_steps: 10
  batch_generation: false  # API doesn't support true batching
  min_step_tokens: 10
  max_step_tokens: 512

# Scorer overrides
scorer:
  max_tokens: 100

# Dataset overrides
dataset:
  subset: 50  # Start small for testing
  offset: 0
  data_name: math
  prompt_file: "${hydra:runtime.cwd}/config/prompts/qwen25_math_official.txt"
