# @package _global_
# Run: CUDA_VISIBLE_DEVICES=0 python scripts/run_tts_eval.py --config-path=../config --config-name=experiments/adaptive_scaling/adaptive_scaling_vllm_qwen25_7.5b_perplexity_math500

defaults:
  - /config
  - /dataset/math_500
  - /model/vllm_qwen25
  - /generation/default
  - /system/default
  - /scorer/perplexity
  - _self_

run_name: "adaptive_scaling_vllm_qwen25_7.5b_perplexity_math500_${now:%H-%M-%S}"

# Model configuration
model:
  type: "vllm"
  model_path: "Qwen/Qwen2.5-7B"
  gpu_memory_utilization: 0.8
  tensor_parallel_size: 1
  enable_prefix_caching: true
  trust_remote_code: true
  max_model_len: 32768
  disable_thinking_mode: true

# System configuration
system:
  device: cuda
  seed: 42

# Generation configuration
generation:
  max_new_tokens: 4096
  temperature: 0.7
  top_p: 0.8
  top_k: 20
  batch_size: 1
  max_length: 8192

# Strategy - Adaptive Scaling
strategy:
  type: adaptive
  candidates_per_step: 4
  max_steps: 50
  scaling_rate: 0.9
  momentum_rate: 0.9
  adaptive_scaling_method: always  # momentum, random, average, always, threshold
  # Detector settings
  detector_type: structured
  min_step_tokens: 50
  max_step_tokens: 300
  use_sequence: true
  use_conclusion: true
  use_thinking: false
  use_verification: true
  use_structure: true
  use_reasoning: true
  use_correction: false
  custom_words:
    - "for example"
    - "given that"
    - "similarly"
    - "step"
    - "therefore"
    - "thus"
  detector_answer_patterns:
    - "<Answer>:"
    - "\n<Answer>:"
  detector_eos_patterns:
    - "<end of response>"

# Dataset configuration
dataset:
  subset: 500
  offset: 0
  prompt_file: "${hydra:runtime.cwd}/config/prompts/default_relaxed_qwen2.5.txt"

# Evaluation configuration
evaluation:
  evaluators:
    - exact_match

# Reporting
report_to: wandb
wandb_project: llm-tts-eval-math500
