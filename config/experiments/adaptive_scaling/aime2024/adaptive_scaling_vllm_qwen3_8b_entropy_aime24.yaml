# @package _global_
# Run: CUDA_VISIBLE_DEVICES=0 python scripts/run_tts_eval.py --config-path=../config --config-name=experiments/adaptive_scaling/adaptive_scaling_vllm_qwen25_7.5b_perplexity_math500

defaults:
  - /config
  - /dataset/aime_2025
  - /model/vllm_qwen3
  - /generation/default
  - /system/default
  - /scorer/entropy
  - _self_

run_name: "adaptive_scaling_vllm_qwen3_8b_entropy_aime24_${now:%H-%M-%S}"

# Model configuration
model:
  type: "vllm"
  model_path: "Qwen/Qwen3-8B"
  gpu_memory_utilization: 0.8
  tensor_parallel_size: 1
  enable_prefix_caching: true
  trust_remote_code: true
  max_model_len: 32768
  disable_thinking_mode: false
  device: "cuda"

# System configuration
system:
  device: cuda
  seed: 42

# Generation configuration
generation:
  max_new_tokens: 4096
  temperature: 0.6
  top_p: 0.95
  top_k: 20
  batch_size: 512
  max_length: 4096

# Strategy - Adaptive Scaling
strategy:
  type: adaptive
  candidates_per_step: 4
  max_steps: 50
  scaling_rate: 0.9
  momentum_rate: 0.9
  adaptive_scaling_method: momentum  # momentum, random, average, always, threshold
  # Detector settings
  detector_type: structured
  min_step_tokens: 50
  max_step_tokens: 4096
  use_sequence: true
  use_conclusion: true
  use_thinking: false
  use_verification: true
  use_structure: true
  use_reasoning: true
  use_correction: false
  custom_words:
    - "for example"
    - "given that"
    - "similarly"
    - "step"
    - "therefore"
    - "thus"
    - "```"
    - "🌈"
    - "\U0001F308"

  stop_token_ids:
    - 151645
    - 151643

# Dataset configuration
dataset:
  subset: 30
  offset: 0
  dataset_path: "test-time-compute/test_aime24"
  dataset_config: null
  dataset_split: "test"
  data_name: math

  system_prompt: "Please reason step by step, and put your final answer within \\boxed{}."
  prompt_template: "{question}"
  prompt_file: null

# Evaluation configuration
evaluation:
  evaluators:
    - exact_match
  batch_evaluators:
    - llm_judge
  llm_judge:
    provider: openai
    model: gpt-4o-mini
    base_url: https://api.openai.com/v1
    cache_path: ./cache/llm_judge
    n_threads: 50

# Reporting
report_to: wandb
wandb_project: llm-tts-eval-aime24
