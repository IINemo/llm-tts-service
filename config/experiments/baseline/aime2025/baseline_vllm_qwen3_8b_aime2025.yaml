# @package _global_
# Run: CUDA_VISIBLE_DEVICES=0 python scripts/run_tts_eval.py --config-path=../config --config-name=experiments/baseline/aime2025/baseline_vllm_qwen3_8b_aime2025
# Baseline evaluation for Qwen3-8B on AIME 2025

defaults:
  - /config
  - /model/vllm_qwen3
  - /generation/default
  - /system/default
  - /scorer/entropy
  - _self_

# Run naming
run_name: "baseline_qwen3_8b_aime2025_${now:%H-%M-%S}"

# Main configuration
verbose: true
report_to: wandb
wandb_project: llm-tts-eval-aime2025

# Model configuration (vLLM backend)
model:
  type: "vllm"
  model_path: "Qwen/Qwen3-8B"
  gpu_memory_utilization: 0.9
  tensor_parallel_size: 1
  enable_prefix_caching: true
  trust_remote_code: true
  max_model_len: 32768
  disable_thinking_mode: true  # Disable thinking mode for baseline

# System configuration
system:
  device: cuda
  seed: 0

# Generation configuration - match official Qwen3 eval parameters
generation:
  max_new_tokens: 32768
  temperature: 0.7
  top_p: 0.8
  top_k: 20
  presence_penalty: 1.5  # Official Qwen3 non-thinking mode parameter
  batch_size: 1

# Strategy - Baseline single-shot
strategy:
  type: baseline
  detector_type: structured
  detector_answer_patterns:
    - "\\boxed{"
  detector_eos_patterns:
    - "</s>"
    - "<|im_end|>"
    - "<|endoftext|>"
  stop_token_ids:
    - 151645
    - 151643

# Dataset configuration - use AIME 2025 dataset (30 problems)
dataset:
  dataset_path: "test-time-compute/aime_2025"
  dataset_config: null
  dataset_split: "test"
  subset: 30
  offset: 0
  question_field: "question"
  answer_field: "answer"
  answer_format: "numeric"
  system_prompt: "Please reason step by step, and put your final answer within \\boxed{}."
  prompt_template: "{question}"
  prompt_file: null

# Evaluation configuration
evaluation:
  evaluators:
    - exact_match
