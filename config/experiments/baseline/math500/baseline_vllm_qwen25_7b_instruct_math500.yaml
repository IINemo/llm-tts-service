# @package _global_
# Run: CUDA_VISIBLE_DEVICES=0 python scripts/run_tts_eval.py --config-path=../config --config-name=experiments/baseline/math500/baseline_vllm_qwen25_7b_instruct_math500
# Baseline single-shot generation (Qwen2.5-7B-Instruct) on MATH-500
# No iterative selection, just generate full response in one call

defaults:
  - /config
  - /dataset/math_500
  - /model/vllm_qwen3
  - /generation/default
  - /system/default
  - /scorer/entropy
  - _self_

# Run naming
run_name: "baseline_vllm_qwen25_7b_instruct_math500_${now:%H-%M-%S}"

# Main configuration
verbose: true
report_to: wandb
wandb_project: llm-tts-eval-math500

# Model configuration (vLLM backend)
model:
  type: "vllm"
  model_path: "Qwen/Qwen2.5-7B-Instruct"
  gpu_memory_utilization: 0.8
  tensor_parallel_size: 1
  enable_prefix_caching: true
  trust_remote_code: true
  max_model_len: 32768
  disable_thinking_mode: true  # No native thinking mode

# System configuration
system:
  device: cuda
  seed: 42

# Generation configuration
generation:
  max_new_tokens: 4096
  temperature: 0.7
  top_p: 0.8
  top_k: 20
  batch_size: 1
  max_length: 8192

# Strategy - Baseline single-shot
strategy:
  type: baseline
  # Use structured detector but only <end of response> as stop token
  detector_type: structured
  # Answer patterns for detector (but not used as stop tokens in baseline)
  detector_answer_patterns:
    - "<Answer>:"
    - "\n<Answer>:"
  # EOS patterns for checking trajectory completion (stop tokens)
  detector_eos_patterns:
    - "<end of response>"

# Dataset configuration
dataset:
  subset: 500  # Full MATH-500 dataset
  offset: 0
  prompt_file: "${hydra:runtime.cwd}/config/prompts/default_relaxed_qwen2.5.txt"

# Evaluation configuration
evaluation:
  evaluators:
    - exact_match
