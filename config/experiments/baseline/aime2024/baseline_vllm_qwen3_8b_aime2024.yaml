# @package _global_
# Run: CUDA_VISIBLE_DEVICES=0 python scripts/run_tts_eval.py --config-path=../config --config-name=experiments/baseline/aime2024/baseline_vllm_qwen3_8b_aime2024
# Baseline evaluation for Qwen3-8B on AIME 2024

defaults:
  - /config
  - /model/vllm_qwen3
  - /generation/default
  - /system/default
  - /scorer/entropy
  - /evaluation/default
  - _self_

# Run naming
run_name: "baseline_qwen3_8b_aime2024_${system.seed}_${now:%H-%M-%S}"

# Main configuration
verbose: true
report_to: wandb
wandb_project: llm-tts-eval-aime2024

# Model configuration (vLLM backend)
model:
  type: "vllm"
  model_path: "Qwen/Qwen3-8B"
  gpu_memory_utilization: 0.9
  tensor_parallel_size: 1
  enable_prefix_caching: true
  trust_remote_code: true
  max_context_budget: 32768
  disable_thinking_mode: true  # Disable thinking mode for baseline

# System configuration
system:
  device: cuda
  seed: 0

# Generation configuration - match official Qwen3 eval parameters
generation:
  max_new_tokens: 32768
  temperature: 0.7
  top_p: 0.8
  top_k: 20
  presence_penalty: 1.5  # Official Qwen3 non-thinking mode parameter
  batch_size: 1

# Strategy - Baseline single-shot
strategy:
  type: baseline
  # No semantic stop tokens for baseline (full response generation)
  stop_token_ids:
    - 151645  # <|im_end|>
    - 151643  # <|endoftext|>
  # Step token limits (required by ThinkingMarkerDetector)
  min_step_tokens: 0
  max_step_tokens: 32768
  # Disable all semantic markers for baseline
  use_sequence: false
  use_conclusion: false
  use_thinking: false
  use_verification: false
  use_structure: false
  use_reasoning: false
  use_correction: false
  # Answer patterns for evaluation
  detector_answer_patterns:
    - "\\boxed{"

# Dataset configuration - use AIME 2024 dataset (30 problems)
dataset:
  dataset_path: "test-time-compute/test_aime24"
  dataset_config: null
  dataset_split: "test"
  subset: 30
  offset: 0
  question_field: "question"
  answer_field: "answer"
  answer_format: "numeric"
  data_name: "aime24"
  system_prompt: "Please reason step by step, and put your final answer within \\boxed{}."
  prompt_template: "{question}"
  prompt_file: null
