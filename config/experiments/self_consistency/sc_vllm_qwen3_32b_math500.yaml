# @package _global_
# Self-Consistency: Qwen3-32B via vLLM on MATH-500
# Tensor parallel across 2 GPUs for 32B model

defaults:
  - /config
  - /dataset/math_500
  - /model/vllm_qwen3
  - /generation/default
  - /system/default
  - _self_

# Run naming
run_name: "math500_sc_vllm_qwen3_32b_n${strategy.num_paths}_${now:%H-%M-%S}"

# Main configuration
verbose: true
report_to: wandb
wandb_project: llm-tts-eval-math500

# Model configuration (vLLM backend with tensor parallelism)
model:
  type: "vllm"
  model_path: "Qwen/Qwen3-32B"
  gpu_memory_utilization: 0.80  # Reduced to leave room for CUDA graphs
  tensor_parallel_size: 2  # Split across both GPUs
  enable_prefix_caching: true
  trust_remote_code: true
  max_model_len: 32768
  disable_thinking_mode: true  # Disable Qwen3 thinking mode
  disable_custom_all_reduce: true  # Workaround for RTX Ada GPUs

# System configuration
system:
  device: cuda
  seed: 42

# Generation configuration (non-thinking mode: temp=0.7, top_p=0.8, top_k=20)
# See: https://huggingface.co/Qwen/Qwen3-32B
generation:
  max_new_tokens: 32768
  temperature: 0.7
  top_p: 0.8
  top_k: 20
  batch_size: 1
  max_length: 8192

# No scorer needed (self-consistency has built-in majority voting)
scorer: null

# Self-consistency strategy
strategy:
  type: self_consistency
  num_paths: 16  # Number of reasoning paths
  max_new_tokens: 32768
  temperature: 0.7
  generation_batch_size: null
  n_threads: 1

# Dataset configuration
dataset:
  subset: 500  # All MATH-500 problems
  offset: 0
  question_field: "problem"
  answer_field: "answer"
  prompt_file: "${hydra:runtime.cwd}/config/prompts/default_relaxed.txt"

# Evaluation configuration
evaluation:
  evaluators:
    - exact_match
