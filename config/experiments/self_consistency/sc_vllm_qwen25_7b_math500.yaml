# @package _global_
# Self-Consistency: Qwen2.5-7B-Instruct via vLLM on MATH-500

defaults:
  - /config
  - /dataset/math_500
  - /model/vllm_qwen3
  - /generation/default
  - /system/default
  - _self_

# Run naming
run_name: "math500_sc_vllm_qwen25_7b_n${strategy.num_paths}_${now:%H-%M-%S}"

# Main configuration
verbose: true
report_to: wandb
wandb_project: llm-tts-eval-math500

# Model configuration (vLLM backend)
model:
  type: "vllm"
  model_path: "Qwen/Qwen2.5-7B"
  gpu_memory_utilization: 0.9
  tensor_parallel_size: 1
  enable_prefix_caching: true
  trust_remote_code: true
  max_model_len: 32768

# System configuration
system:
  device: cuda
  seed: 42

# Generation configuration
generation:
  max_new_tokens: 32768
  temperature: 0.7
  top_p: 0.8
  top_k: 20
  batch_size: 1
  max_length: 8192

# No scorer needed
scorer: null

# Self-consistency strategy
strategy:
  type: self_consistency
  num_paths: 16
  max_new_tokens: 32768
  temperature: 0.7
  generation_batch_size: null
  n_threads: 1

# Dataset configuration
dataset:
  subset: 500
  offset: 0
  question_field: "problem"
  answer_field: "answer"
  prompt_file: "${hydra:runtime.cwd}/config/prompts/default.txt"

# Evaluation configuration
evaluation:
  evaluators:
    - exact_match
