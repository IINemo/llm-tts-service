# @package _global_
# Run: CUDA_VISIBLE_DEVICES=0 python scripts/run_tts_eval.py --config-path=../config --config-name=experiments/self_consistency/aime2024/self_consistency_vllm_thinking_qwen3_8b_aime2024
# Self-Consistency with vLLM + Thinking Mode (Qwen3-8B) on AIME 2024
# Uses batch generation with majority voting over multiple reasoning paths

defaults:
  - /config
  - /model/vllm_qwen3
  - /generation/default
  - /system/default
  - /scorer/entropy
  - _self_

# Run naming
run_name: "self_consistency_vllm_thinking_qwen3_8b_aime2024_${now:%H-%M-%S}"

# Main configuration
verbose: true
report_to: wandb
wandb_project: llm-tts-eval-aime2024

# Model configuration (vLLM backend)
model:
  type: "vllm"
  model_path: "Qwen/Qwen3-8B"
  gpu_memory_utilization: 0.8
  tensor_parallel_size: 1
  enable_prefix_caching: true
  trust_remote_code: true
  max_model_len: 32768
  disable_thinking_mode: false  # ENABLE native thinking mode

# System configuration
system:
  device: cuda
  seed: 42

# Generation configuration - thinking mode settings
generation:
  max_new_tokens: 32768
  temperature: 0.6
  top_p: 0.95
  top_k: 20
  batch_size: 1

# Strategy - Self-Consistency (batch generation with majority voting)
strategy:
  type: self_consistency
  num_paths: 8  # Number of reasoning paths (generated in single vLLM call)

# Dataset configuration - AIME 2024 (30 problems)
dataset:
  dataset_path: "test-time-compute/test_aime24"
  dataset_config: null
  dataset_split: "test"
  subset: 30
  offset: 0
  question_field: "question"
  answer_field: "answer"
  answer_format: "numeric"
  data_name: "aime2024"
  system_prompt: "Please reason step by step, and put your final answer within \\boxed{}."
  prompt_template: "{question}"
  prompt_file: null

# Evaluation configuration
evaluation:
  evaluators:
    - exact_match
  batch_evaluators:
    - llm_judge
  llm_judge:
    provider: openai
    model: gpt-4o-mini
    base_url: https://api.openai.com/v1
    cache_path: ./cache/llm_judge
    n_threads: 50
