# @package _global_
# Run: CUDA_VISIBLE_DEVICES=0 python scripts/run_tts_eval.py --config-path=../config --config-name=experiments/self_consistency/aime2025/self_consistency_vllm_thinking_qwen3_8b_aime2025
# Note: Do NOT specify CUDA_VISIBLE_DEVICES when submitting via scripts/slurm/submit.sh - SLURM assigns GPUs automatically
# Self-Consistency with vLLM + Thinking Mode (Qwen3-8B) on AIME 2025
# Uses batch generation with majority voting over multiple reasoning paths

defaults:
  - /config
  - /dataset/aime_2025
  - /model/vllm_qwen3
  - /generation/default
  - /system/default
  - /scorer/entropy
  - /evaluation/default
  - _self_

# Run naming
run_name: "self_consistency_vllm_thinking_qwen3_8b_aime2025_seed${system.seed}_${now:%H-%M-%S}"

# Main configuration
verbose: true
report_to: wandb
wandb_project: llm-tts-eval-aime2025
wandb_group: "self_consistency_qwen3_8b_thinking_aime2025"

# Model configuration (vLLM backend)
model:
  type: "vllm"
  model_path: "Qwen/Qwen3-8B"
  model_short_name: "vllm_thinking_qwen3_8b"
  gpu_memory_utilization: 0.85
  tensor_parallel_size: 1
  enable_prefix_caching: true
  trust_remote_code: true
  max_context_budget: 32768
  disable_thinking_mode: false  # ENABLE native thinking mode

# System configuration
system:
  device: cuda
  seed: 42

# Generation configuration - thinking mode settings
generation:
  max_new_tokens: 32768
  temperature: 0.6
  top_p: 0.95
  top_k: 20
  batch_size: 1
  checkpoint_batch_size: 4

# Strategy - Self-Consistency (batch generation with majority voting)
strategy:
  type: self_consistency
  num_paths: 8  # Number of reasoning paths (generated in single vLLM call)

# Dataset configuration
dataset:
  subset: 30  # Full AIME-2025 dataset
  offset: 0
  data_name: aime25
  system_prompt: "Please reason step by step, and put your final answer within \\boxed{}."
